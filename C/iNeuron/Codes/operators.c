#include<stdio.h>

int main(){
    /*
    int x =5, y;
    y = x++;
    printf("x : %d, y : %d\n",x,y);

    printf("\n***********************************************************************************\n");

    int p =5, q;
    q = ++p;
    printf("p : %d, q : %d",p,q);

    printf("\n***********************************************************************************\n");

    int short s = 221;
    printf("\n s : %d and size : %d", s, sizeof(s));

    printf("\n***********************************************************************************\n");

    long int l = 1234567891;
    printf("\n l : %d and size : %d", l, sizeof(l));
    printf("\nBut the size of long int is : %d",sizeof(long int));
    printf("\nBut the size of long double is : %d",sizeof(long double));
    printf("\nBut the size of integer constant is int so 35 : %d",sizeof(35));
    printf("\nBut the size of real constant is double so 3.5 : %d",sizeof(3.5));

    printf("\n***********************************************************************************\n");
    */
    int x = 12;
    int r = x / 2;
    int t = x * 2;
    printf("12 Divide by 2 : %d\n",r);
    printf("12 Multiply by 2 : %d",t);

    return 0;
}
