#include<stdio.h>

int main(){

    int arr[] = {10, 20, 30, 40, 30,20, 20, 20, 30, 40, 50, 60};
    int i,c,j;
    int size_array = sizeof(arr)/ sizeof(arr[0]);
    for(i = 0; i < size_array; i++){
        c = 0;
        for(j = i+1; j < size_array; j++){
            if()
        }
    }

    return 0;
}
